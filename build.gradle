plugins {
    id 'maven-publish'
}

// Esto le dice a JitPack que el grupo es tu usuario
group = 'com.github.P4drote'
version = '1.0.1'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.P4drote'
            artifactId = 'device-sdk'
            version = '1.0.1'
            
            // Aquí le decimos que el "producto" es el archivo AAR
            artifact("libs/DeviceAPI_ver20231208_release.aar")
        }
    }
}