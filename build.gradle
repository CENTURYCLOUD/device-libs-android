plugins { id 'maven-publish' }
group = 'com.github.CENTURYCLOUD.zebra-reader' // Grupo Único
version = '1.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.CENTURYCLOUD.zebra-reader'
            artifactId = 'reader'
            version = '1.0.0'
            artifact("libs/API3_READER-release-2.0.2.124.aar")
        }
    }
}
task clean(type: Delete) { delete rootProject.buildDir }
task assemble {}